// frontend/src/services/firebase.js
import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";
import { getStorage } from "firebase/storage";

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyC8qpABejeVyHAivn63NMHT20BXtUEuY6E",
  authDomain: "mutindo-catering.firebaseapp.com",
  projectId: "mutindo-catering",
  storageBucket: "mutindo-catering.firebasestorage.app",
  messagingSenderId: "698847723803",
  appId: "1:698847723803:web:4e3740826257f058f613be"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

// Initialize Firestore (database for text content)
export const db = getFirestore(app);

// Initialize Storage (alternative for images - though you're using Cloudinary)
export const storage = getStorage(app);

export default app;
